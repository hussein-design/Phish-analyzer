api:
  # ─────────────────────────────────────────────────────────────────────────
  # LEGACY CLI ONLY (main.py).  The desktop app NEVER reads this file.
  # Desktop app API keys are stored exclusively in the Settings dialog
  # (persisted to the local SQLite DB at %LOCALAPPDATA%\PhishAnalyzer\...).
  #
  # WARNING: Any keys you put here will end up in git history if committed.
  # Do not commit real keys.  Rotate any key that was ever stored here.
  # ─────────────────────────────────────────────────────────────────────────
  virustotal_key: ""
  abuseipdb_key: ""

scoring:
  brand_mismatch: 2
  spf_fail: 2
  dkim_fail: 2
  dmarc_fail: 2
  header_issue: 1
  url_bad_keyword: 2
  url_ip_host: 3
  attachment_executable: 3
  vt_malicious_threshold: 3
  vt_malicious_points: 4
  abuseipdb_high_score: 50
  abuseipdb_points: 3

brand_domains:
  microsoft:
    - microsoft.com
    - live.com
    - office.com
  paypal:
    - paypal.com
  "sam's club":
    - samsclub.com

url_suspicious_keywords:
  - login
  - verify
  - update
  - password
  - secure
  - invoice
