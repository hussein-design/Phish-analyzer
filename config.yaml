api:
  # IMPORTANT: the keys that were here have been rotated and removed.
  # This file is only used by the legacy CLI (main.py) — the desktop app
  # stores keys in the Settings dialog (saved to the local SQLite DB).
  # If you use main.py, paste your own keys here. Never commit real keys.
  virustotal_key: ""
  abuseipdb_key: ""

scoring:
  brand_mismatch: 2
  spf_fail: 2
  dkim_fail: 2
  dmarc_fail: 2
  header_issue: 1
  url_bad_keyword: 2
  url_ip_host: 3
  attachment_executable: 3
  vt_malicious_threshold: 3
  vt_malicious_points: 4
  abuseipdb_high_score: 50
  abuseipdb_points: 3

brand_domains:
  microsoft:
    - microsoft.com
    - live.com
    - office.com
  paypal:
    - paypal.com
  "sam's club":
    - samsclub.com

url_suspicious_keywords:
  - login
  - verify
  - update
  - password
  - secure
  - invoice
